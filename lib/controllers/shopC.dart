import 'package:get/get.dart';

class ShopController extends GetxController {
  // var count = 0.obs;

  var quantity = 0.obs;
  var total = 0.obs;

  // void kurang() => count--;
  // void tambah() => count++;
}
